apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: dev-matrix
  namespace: openshift-gitops
spec:
  generators:
    - matrix:
        generators:
          # Generador 1: Lee todas las aplicaciones desde Git
          - git:
              repoURL: https://github.com/kikan321/hello-openshift-gitops.git
              revision: main
              directories:
                - path: dev/*
          # Generador 2: Lee todos los clusters seleccionados por RHACM
          - clusterDecisionResource:
              configMapRef: dev-placement
              labelSelector:
                matchLabels:
                  cluster.open-cluster-management.io/placement: dev-placement
              requeueAfterSeconds: 30
  template:
    metadata:
      name: '{{path.basename}}-{{name}}'
    spec:
      project: default
      destination:
        namespace: '{{name}}'
        server: '{{server}}'
      source:
        repoURL: https://github.com/kikan321/hello-openshift-gitops.git
        path: '{{path}}'
        helm:
          valueFiles:
            - values-dev.yaml
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
